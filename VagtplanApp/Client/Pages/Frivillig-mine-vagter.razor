@page "/frivillig-mine-vagter"
@using VagtplanApp.Client.Services

<h3>Mine vagter</h3>

@if (vagter == null)
{
    <p><em>Henter vagter...</em></p>
}
else if (!vagter.Any())
{
    <p>Du har ikke ingen vagter.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Start Tid</th>
                <th>Slut Tid</th>
                <th>Minimums Alder</th>
                <th>Antal Personer</th>
                <th>Fjern vagt</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var vagt in vagter)
            {
                <tr>
                    <td>@vagt.startTime</td>
                    <td>@vagt.endTime</td>
                    <td>@vagt.minAge</td>
                    <td>@vagt.numberOfPersons</td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private List<Shift> vagter;

    [Inject]
    private IShiftService mService { get; set; }

    protected override async Task OnInitializedAsync()
    {
        vagter = await mService.GetShiftsForVolunteer();
    }
}
